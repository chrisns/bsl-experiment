/**
 * Standalone configuration for GitHub Pages deployment.
 * In the full AWS deployment, this file is generated by the seed Lambda
 * with API endpoints and Cognito configuration. In standalone mode,
 * server-side features are disabled and all inference runs in-browser.
 */
window.__BSL_CONFIG__ = {
    enableServerRecognition: false,
    standalone: true
};
